source 'https://rubygems.org'

gem 'rails', '~> 5.0.0', '>= 5.0.0.1'
gem 'pg', '~> 0.18'                    # Use postgresql as the database for Active Record
gem 'puma', '~> 3.0'                   # Use Puma as the app server
gem 'sass-rails', '~> 5.0'             # Use SCSS for stylesheets

gem 'uglifier', '>= 1.3.0'             # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.2'           # Use CoffeeScript for .coffee assets and views

# gem 'therubyracer', platforms: :ruby   # See https://github.com/rails/execjs#readme for more supported runtimes

gem 'jquery-rails'                     # Use jquery as the JavaScript library

gem 'turbolinks', '~> 5'               # Turbolinks makes navigating your web application faster.
gem 'jbuilder', '~> 2.5'               # Build JSON APIs with ease.
gem 'nokogiri'                         # Nokogiri (鋸) is an HTML, XML, SAX, and Reader parser with XPath and CSS selector support.

gem 'activerecord-import'              # library for bulk inserting data using ActiveRecord

# gem 'redis', '~> 3.0'                  # Use Redis adapter to run Action Cable in production
# gem 'bcrypt', '~> 3.1.7'               # Use ActiveModel has_secure_password

gem 'bootstrap-sass'                   # popular HTML, CSS, and JS framework

gem 'chartkick'                        # Create beautiful Javascript charts with one line of Ruby
gem 'highcharts'                       # Highcharts makes it easy for developers to set up interactive charts in their web pages
gem 'jquery-datatables-rails'          # advanced interaction controls for HTML tables

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  gem 'pry'
end

group :development do
  gem 'binding_of_caller'
  gem 'better_errors'
  gem 'web-console'        # Access an IRB console on exception pages or by using <%= console %> anywhere in the code.
  gem 'listen', '~> 3.0.5'
  gem 'spring'             # Spring speeds up development by keeping your application running in the background.
  gem 'spring-watcher-listen', '~> 2.0.0'
end
